# v0.1.3

Decoupled `FFPlayer` from the renderer in `ffplay-gui` and introduced a basic API.

- Added **D3D11 renderer** + **D3D11 GUI build** (`ffplay-gui-d3d11.exe`).
- Refactored filter pipeline: no longer depends on `SDL_RenderInfo`.
- Refactored demuxer boundary.
